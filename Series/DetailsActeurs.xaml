<Page
    x:Class="Series.DetailsActeurs"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Series"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Button x:Name="BouttonRetour" Content="Retour accueil" Click="ClickBouttonRetour" HorizontalAlignment="Left" Height="47" Margin="40,40,0,0" VerticalAlignment="Top" Width="176"/>
        <ListView x:Name="ListeSeries" HorizontalAlignment="Left" Height="351" Margin="266,140,0,0" VerticalAlignment="Top" Width="450" SelectionChanged="SerieSelectionnee">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <Image Source="{Binding image.medium}" Height="80"></Image>
                        <TextBlock Grid.Column="1" Text="{Binding name}" 
                           Margin="20,0,20,8"
                           FontSize="20"  
                           VerticalAlignment="Center"
                           FontWeight="SemiBold"
                           Foreground="DarkBlue"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <TextBox x:Name="TitrePage" Text="Titre"  HorizontalAlignment="Center" Height="67" Margin="0,40,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="456" FontWeight="Bold" TextAlignment="Center" FontSize="48"/>
        <Image x:Name="PhotoActeur" Source="{x:Bind ImageUrl}" HorizontalAlignment="Left" Height="269" Margin="40,140,0,0" VerticalAlignment="Top" Width="198"/>

    </Grid>
</Page>
